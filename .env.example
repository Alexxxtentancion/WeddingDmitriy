# Yandex Maps API key (https://developer.tech.yandex.ru/)
VITE_YANDEX_MAPS_KEY=your_yandex_maps_api_key

# RSVP deadline (YYYY-MM-DD), must match frontend weddingConfig.rsvpDeadline
RSVP_DEADLINE=2026-08-15

# Rate limit: max requests per IP per minute
RATE_LIMIT_PER_MINUTE=10

# CSV file path inside backend container
CSV_PATH=/app/data/rsvp.csv

# CORS origins (comma-separated, use * for dev)
CORS_ORIGINS=*
